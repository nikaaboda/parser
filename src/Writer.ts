type Filetype = "ORDER" | "INVOICE" | "DEASDV";
type ReadFormat = "XML" | "JSON";

export class Writer {
    write(file: string, fileType: Filetype, isPath = false, readFormat: ReadFormat = "JSON") {

    }
}